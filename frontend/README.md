Frontend (React + Vite)

Install & run:

```powershell
cd frontend
npm install
npm run dev
```

This app expects the backend API at `/api/` (same host). Use a proxy if running frontend on different port during development.
